export const environment = {
  production: true,
  BACKEND_URL: `https://${window.location.hostname}`,
  profile: null,
  userApiRoot: 'lil',
  uiUrl: "https://lille.in",
  header: {headers: {'Content-Type': 'application/json'}}
};